#!/usr/bin/env bash
#
# copy-pulse-assets.sh — REMOVED in v0.57.0
#
# Pulse assets (pulse.sh, pulse.ps1, prompts, SETUP.md) are no longer
# bundled with the extension; the host owns scheduling instead.
#
# This stub remains so the npm `build` script (which still calls it for
# backward compatibility) is a no-op. Exit 0.
#

echo "copy-pulse-assets: pulse removed in v0.57.0 — nothing to copy."
exit 0