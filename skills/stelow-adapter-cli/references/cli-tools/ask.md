# ask_user_question

Use the canonical `ask_user_question` tool for human input. Hosts may map this
name to their native interaction API (Fusion uses `fn_ask_question`). Do not
invoke a host-specific command directly from a skill.
